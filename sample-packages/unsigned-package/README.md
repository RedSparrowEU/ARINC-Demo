# Unsigned package fixture

- Data classification: generated test fixture data.
- Scenario: a device requires signing but the demo package is unsigned.
- Expected validation: failed.
- Expected diagnostics: blocking `signature required` issue.

This fixture contains no real keys or signatures. It is non-operational, not certified, and must not
be used for navigation or flight.
